
;; ------------------------
;; Tags & Attributes
;; ------------------------

(element
  (start_tag
    (tag_name) @tag.name))

(element
  (self_closing_tag
    (tag_name) @tag.name))

(attribute
  (attribute_name) @attribute.name)