
;; ------------------------
;; Selectors & Properties
;; ------------------------

(class_selector
  (class_name) @class.name)

(id_selector
  (id_name) @property.name)

(tag_name) @tag.name

(declaration
  (property_name) @property.name)